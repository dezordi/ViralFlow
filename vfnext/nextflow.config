params {
  outDir = "$PWD/"
  inDir = "$PWD/"
  referenceGenome = "$PWD/reference.fasta"
  threads = 1
  minLen = 75
  depth = 5
  minDpIntrahost = 100
  trimLen = 0
  adaptersFile = "$PWD/input/ART_adapters.fa"

  // container specific

}

process.container = "/home/ubuntu/Projects/git_stuff/ViralFlow/viralflow_container"

singularity {
    enabled = false
    autoMounts = true
    //cacheDir = "$PWD"
    //runOptions = "--bind /lustre,/nfs,/software"
}
